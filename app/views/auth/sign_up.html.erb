<div style="display: flex; justify-content: center; align-items: center; min-height: calc(100vh - 200px);">
  <div class="border rounded p-6" style="min-width: 400px">
    <%= render(Primer::Beta::Heading.new(tag: :h1, mb:4)) { t('views.auth.sign_up_title') } %>
    <%= primer_form_with(url: auth_sign_up_path, model: @user) do |f| %>
      <%= render(SignUpForm.new(f)) %>
    <% end %>
  </div>
</div>
